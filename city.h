#ifndef CITY_H
#define CITY_H
#include <iostream>
#include <string>
#include <vector>
using namespace std;

void City(int height, int width, vector<string>& raw_city, string**& city_map, vector<string> &raw_name, vector<string> &raw_id);
#endif
